rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Standard rule for Hermona AI: Allow authenticated users to read and write their own data
    // and common collections (forum, chat_history, detections, etc.)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
